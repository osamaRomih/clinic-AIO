export interface ILoginResponse {
  email: string;
  expiresIn: string;
  firstName: string;
  id: string;
  lastName: string;
  refreshToken: string;
  refreshTokenExpiration: string;
  token: string;
}
